public class City {

    private CityLocation[][] spreadsheet;

    public City() {

    }

    public City(String filename) {
        this();
    }

    public void LoadSpreadsheet() {
        //In the spreadsheet we will see
        //A1 is green, A2 is yellow
        if (spreadsheet is green){
            new CityLocation(0);
        } else {
            new CityLocation(1);
        }
    }

}
